<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*"/>
            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>

        <Image x:Name="img" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <Button x:Name="sendOrCancelBtn" Grid.Row="1" Grid.Column="1" Content="Send" Click="sendOrCancelBtn_Click" />
        <ComboBox x:Name="filterComboBox" Grid.Row="2" Grid.Column="1"/>
        <Button x:Name="selectImgBtn" Grid.Row="1" Grid.Column="0" Content="Select Image" Click="selectImgBtn_Click" Grid.RowSpan="2" />
        <ProgressBar x:Name="progressBar" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3"/>
        

    </Grid>
</Window>
